package com.staykonkan.amenity.entity;

/** Grouping used to organize amenities for display (e.g. filter chips, property-form sections). */
public enum AmenityCategory {
    BASIC,
    ROOM,
    KITCHEN,
    BATHROOM,
    OUTDOOR,
    ENTERTAINMENT,
    SAFETY,
    VIEW,
    OTHER
}
